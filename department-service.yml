spring:
  datasource:
    url: jdbc:mysql://localhost:3306/departments?useSSL=false&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10     # Maksimum e? zamanl? ba?lant? say?s?
      minimum-idle: 2           # Haz?rda bekleyen minimum ba?lant?
      idle-timeout: 30000       # Bo?ta kalan ba?lant?n?n kapat?lma s�resi (ms)
      max-lifetime: 1800000     # Ba?lant?n?n maksimum �mr� (ms)
      connection-timeout: 20000 # Ba?lant? iste?inin zaman a??m? s�resi (ms)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
info:
  app:
    name: department-services microservices workshop
    description: microservices springboot workshop
    version: 0.0.1
server:
  port: 8080
  
